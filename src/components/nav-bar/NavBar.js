import "./navBar.css";
import Categories from "../categories/categories";
const NavBar = () => {
    return (
        <nav>
            <Categories></Categories>
        </nav>
    );
}

export default NavBar;