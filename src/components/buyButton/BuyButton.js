import "./buyButton.css";

const BuyButton = props => {
    console.log(props);
    return (
        <button className="buy-button">
            Buy
        </button>
    );
}

export default BuyButton;